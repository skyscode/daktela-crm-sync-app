<?php
/**
 * tests/SyncServiceTest.php — SyncService orchestration tests
 *
 * Tests that SyncService correctly orchestrates one full sync cycle:
 * fetches from the (mocked) Daktela API and upserts into the (in-memory SQLite) DB.
 */
